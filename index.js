import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(<h1>My Man!</h1>, document.getElementById('root'));

// might need to keep up with localhost:3000 on browser. VSCode doesn't seem to open into it.
